// Boolean
let flag: boolean = true;
console.log("Value assigned to the flag is: " + flag);

// Number
function addition(value: string) {
    console.log("Total is: " + value);
    }
    let num1: number = 8;
    let num2: number = 2;
    let total: string = (num1 + num2).toLocaleString();

    addition(total);

// String
let firstName: string = "Connor";
console.log("Hello my name is " + firstName);

// Array
let list: Array<number> = [1,2,3,4];
console.log("This array contains " + list);